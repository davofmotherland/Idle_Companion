# Default Seal

Default Project Seal character pack.

Current M3 status:

- Runtime uses procedural placeholder art.
- Visual parameters are driven by `manifest.json`.
- Source/metadata is tracked in `art-metadata.json`.

Final art requirements before Steam candidate:

- `default_seal.aseprite`
- `default_seal_sheet.png`
- `preview.png`
- Updated `manifest.json` animation mappings
