# Gray Test Seal

This is an intentionally incomplete character pack for M3 QA.

It verifies:

- A second manifest can be discovered.
- Character switching works without code changes.
- Missing animation files do not crash the runtime because procedural fallback is enabled.
